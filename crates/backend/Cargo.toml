[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5", features = ["json"] }
shared = { path = "../shared" }
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite", "migrate"] }
